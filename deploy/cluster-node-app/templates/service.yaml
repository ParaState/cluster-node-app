apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-node-app.fullname" . }}
  labels:
    {{- include "cluster-node-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cluster-node-app.selectorLabels" . | nindent 4 }}
